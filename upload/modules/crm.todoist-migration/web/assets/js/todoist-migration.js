(() => {
  'use strict';
  const api = (path, options = {}) => fetch(`../api/index.php?route=api/v1/modules/crm.todoist-migration${path}`, { headers: { 'Content-Type': 'application/json', Accept: 'application/json' }, ...options }).then(async response => { const data = await response.json().catch(() => ({})); if (!response.ok || data.success === false) throw new Error(data.error?.message || data.message || 'Ошибка API'); return data.data || data; });
  const $ = id => document.getElementById(id); let selectedConnection = null; let projects = [];
  const message = (text, bad = false) => { const el = $('todoistActionMessage'); if (el) { el.textContent = text; el.className = `small mt-3 ${bad ? 'text-danger' : 'text-success'}`; } };
  const renderConnections = items => { const root = $('todoistConnections'); root.innerHTML = ''; if (!items.length) { root.textContent = 'Подключений пока нет.'; return; } items.forEach(item => { const row = document.createElement('div'); row.className = 'todoist-connection'; row.innerHTML = `<div><strong>${escapeHtml(item.name)}</strong><div class="text-muted small">${escapeHtml(item.account_name || item.status || '')}</div></div><button class="btn btn-sm crm-btn-secondary">Выбрать</button>`; row.querySelector('button').addEventListener('click', () => selectConnection(item)); root.appendChild(row); }); };
  const selectConnection = item => { selectedConnection = item; $('todoistDiscover').disabled = false; $('todoistStart').disabled = false; loadProjects(); message(`Выбрано: ${item.name}`); };
  const loadConnections = () => api('/connections').then(data => renderConnections(data.connections || data.items || [])).catch(error => { $('todoistConnections').textContent = error.message; });
  const loadProjects = () => { if (!selectedConnection) return; $('todoistProjects').textContent = 'Загрузка проектов…'; return api(`/connections/${encodeURIComponent(selectedConnection.public_id)}/projects`).then(data => { projects = data.projects || []; const root = $('todoistProjects'); root.innerHTML = ''; if (!projects.length) { root.textContent = 'Проектов не найдено.'; return; } projects.forEach(project => { const row = document.createElement('label'); row.className = 'todoist-project'; row.innerHTML = `<input type="checkbox" value="${escapeHtml(String(project.id))}" checked> <span>${escapeHtml(project.name || 'Без названия')}</span>`; root.appendChild(row); }); }).catch(error => { $('todoistProjects').textContent = error.message; }); };
  const loadJobs = () => api('/jobs').then(data => { const root = $('todoistJobs'); const items = data.items || []; root.innerHTML = items.length ? items.map(job => `<div class="todoist-connection"><div><strong>${escapeHtml(job.connection_name || 'Todoist')}</strong><div class="text-muted small">${escapeHtml(job.current_step || '')}</div></div><span class="todoist-status">${escapeHtml(job.status || '')}</span></div>`).join('') : 'Jobs пока нет.'; }).catch(error => { $('todoistJobs').textContent = error.message; });
  const addConnection = () => { const modal = bootstrap.Modal.getOrCreateInstance($('todoistConnectionModal')); $('todoistConnectionForm').reset(); $('todoistConnectionError').classList.add('d-none'); modal.show(); };
  const submitConnection = event => { event.preventDefault(); const form = new FormData(event.currentTarget); const error = $('todoistConnectionError'); const oauth = form.get('auth_type') === 'oauth2'; const payload = oauth ? { name: form.get('name'), client_id: form.get('client_id'), client_secret: form.get('client_secret'), code: form.get('code'), state: form.get('state'), redirect_uri: form.get('redirect_uri') } : { name: form.get('name'), access_token: form.get('access_token') }; api(oauth ? '/oauth/exchange' : '/connections', { method: 'POST', body: JSON.stringify(payload) }).then(data => { bootstrap.Modal.getInstance($('todoistConnectionModal'))?.hide(); loadConnections(); if (data.connection) selectConnection(data.connection); }).catch(e => { error.textContent = e.message; error.classList.remove('d-none'); }); };
  const authorizeOAuth = () => { const form = new FormData($('todoistConnectionForm')); api('/oauth/authorize-url', { method: 'POST', body: JSON.stringify({ client_id: form.get('client_id'), redirect_uri: form.get('redirect_uri') }) }).then(data => { if (data.authorization_url) window.open(data.authorization_url, '_blank', 'noopener'); message('Authorization URL открыт в новой вкладке. После callback заполните code и state.'); }).catch(e => { const error = $('todoistConnectionError'); error.textContent = e.message; error.classList.remove('d-none'); }); };
  const createJob = () => { if (!selectedConnection) return; const projectIds = [...document.querySelectorAll('#todoistProjects input:checked')].map(input => input.value); const options = { include_completed: $('todoistCompleted').checked, include_comments: $('todoistComments').checked, include_attachments: $('todoistAttachments').checked }; api('/jobs', { method: 'POST', body: JSON.stringify({ connection_public_id: selectedConnection.public_id, project_ids: projectIds, mode: $('todoistMode').value, target_options: options }) }).then(() => { message('Job создан и поставлен в очередь после запуска.'); loadJobs(); }).catch(e => message(e.message, true)); };
  const escapeHtml = value => String(value).replace(/[&<>'"]/g, char => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#039;', '"': '&quot;' }[char]));
  document.addEventListener('DOMContentLoaded', () => { $('todoistAddConnection')?.addEventListener('click', addConnection); $('todoistConnectionForm')?.addEventListener('submit', submitConnection); $('todoistOAuthAuthorize')?.addEventListener('click', authorizeOAuth); $('todoistAuthType')?.addEventListener('change', event => { const oauth = event.target.value === 'oauth2'; $('todoistPatFields')?.classList.toggle('d-none', oauth); $('todoistOauthFields')?.classList.toggle('d-none', !oauth); }); $('todoistDiscover')?.addEventListener('click', loadProjects); $('todoistStart')?.addEventListener('click', createJob); $('todoistRefreshJobs')?.addEventListener('click', loadJobs); loadConnections(); loadJobs(); });
})();
