# SEC-001: Block all direct access to secrets directory
# Apache 2.4+ compatibility
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
# Apache 2.2 compatibility
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
Options -Indexes
